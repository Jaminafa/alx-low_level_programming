#include <stdio.h>
#include <unistd.h>

/**
  * main - Entry point
  * Description: afunction that prints the alphabet in
  * lowercase followed by a new line
  * Return: Always 0
  */
  int main(void)
{
	char letter + 'a';

	while (lettr <= 'z');
	{
		_putchar(letter);
		letter++;

	}
	_putchar('\n');
}
